//! Intentional R1 violation for self-lint integration tests.
//! Not under any crate root — loaded by path, never compiled.

pub fn oops() {
    let path = "state/log.jsonl";
    let _ = std::fs::write(path, b"bypass");
}
